# Get a List: Write a program which continuously asks the user to enter values which are added one by one into a list. When the user presses enter without typing anything, print the list.

def get_user_list():
# Initialize an empty list    

    lst = []
# Starty an infinite loop to ask the user for input    

    while True:
        
        # Prmpt the user to enter a value
        
        value = input("Enter a value: ")
        
        # If the user presses Enter without typing anything, exit the loop
        
        if value == "":
            break
        
        # Append the entered value to the list
        
        lst.append(value) 
        
     # After existng loop, print the result with all the entered values 
     
    print("Here's the list:", lst)

# Call the function to run the program    
get_user_list()



